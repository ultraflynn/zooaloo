"use strict"

// Tokens hold these for both "Cleaning" and "Feeding".

module.exports = () => {
  return {
    cards() {
      return [
        "Big Cat", "Bird", "Monkey", "Insert", "African Animals", "Reptile"
      ]
    }
  }
}
